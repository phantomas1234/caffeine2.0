import { PlaceholderPage } from "@/components/PlaceholderPage";

export default function Page() {
  return (
    <PlaceholderPage
      title="Jobs"
      phase={4}
      description="Async jobs (metabolic-ninja pathway prediction); backed by pg-boss + Modal worker."
    />
  );
}
