import { PlaceholderPage } from "@/components/PlaceholderPage";

export default function Page() {
  return (
    <PlaceholderPage
      title="Projects"
      phase={1}
      description="Workspace for grouping models, designs, and experiments."
    />
  );
}
