import { PlaceholderPage } from "@/components/PlaceholderPage";

export default function Page() {
  return (
    <PlaceholderPage
      title="Models"
      phase={1}
      description="Metabolic models (SBML). Upload validates via the model-parser Modal service."
    />
  );
}
