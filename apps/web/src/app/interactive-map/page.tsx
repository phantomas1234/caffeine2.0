import { PlaceholderPage } from "@/components/PlaceholderPage";

export default function Page() {
  return (
    <PlaceholderPage
      title="Interactive Map"
      phase={2}
      description="Escher map editor; drives the design + simulation workflow."
    />
  );
}
