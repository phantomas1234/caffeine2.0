import { PlaceholderPage } from "@/components/PlaceholderPage";

export default function Page() {
  return (
    <PlaceholderPage
      title="Experiments"
      phase={5}
      description="Experimental data + proteomics & fermentation uploads."
    />
  );
}
