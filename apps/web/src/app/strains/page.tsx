import { PlaceholderPage } from "@/components/PlaceholderPage";

export default function Page() {
  return (
    <PlaceholderPage
      title="Strains"
      phase={5}
      description="Strain registry (lineage of modifications applied to organisms)."
    />
  );
}
